RESTful API for an address book which uses an Elasticsearch data store.

This project uses a virtualenv named adbook.
A list of modules needed for running this project can be found in the requirements.txt file. 